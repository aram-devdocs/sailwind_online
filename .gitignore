# Game IP and local machine state. Never commit (game DLLs, Steam content).
lib/
.sandbox/
decompiled/
.tools/

# Build output
artifacts/
bin/
obj/
target/
server/target/

# Local agent state (project agent config IS committed; local settings are not)
.claude/settings.local.json
# Per-session hook state (role marker, one-time-block flags); runtime-only.
.claude/state/

# Editor and user files
*.user

# Logs and OS noise
*.log
.DS_Store
Thumbs.db

# Local, never-committed agent state (leak blocklist lives here)
.agents/local/
