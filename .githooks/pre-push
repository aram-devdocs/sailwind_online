#!/usr/bin/env bash
# Full local gate before code leaves the machine. Runs the same fast gate CI
# runs, so a green push means the same thing everywhere.
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

make validate
